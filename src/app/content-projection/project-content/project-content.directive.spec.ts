import { ProjectContentDirective } from './project-content.directive';

describe('ProjectContentDirective', () => {
  it('should create an instance', () => {
    const directive = new ProjectContentDirective();
    expect(directive).toBeTruthy();
  });
});
